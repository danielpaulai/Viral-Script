# PASTE THIS ENTIRE PROMPT INTO REPLIT AI NOW:

---

I have two critical errors in production that need to be fixed immediately:

## ERROR 1: 403 Forbidden
```
GET https://viralscript.co/api/knowledge-base 403 (Forbidden)
```

## ERROR 2: 500 Internal Server Error
```
POST https://viralscript.co/api/solutions/generate 500 (Internal Server Error)
{"error":"Failed to generate solutions. Please try again."}
```

## WHAT I NEED YOU TO DO:

### STEP 1: Fix the 403 on /api/knowledge-base

The 403 means the route either:
- Doesn't exist in production
- Has authentication blocking it
- Has CORS issues

Find the `/api/knowledge-base` route and:

1. Make sure it exists and is exported correctly
2. Add proper error handling
3. Make sure it doesn't require auth for GET requests (or handle auth properly)

```javascript
// Example fix:
app.get('/api/knowledge-base', async (req, res) => {
  try {
    // If auth is required, check it
    // If not, just return empty array for now
    const knowledgeBase = []; // or fetch from database
    res.json(knowledgeBase);
  } catch (error) {
    console.error('Knowledge base error:', error);
    res.status(500).json({ error: 'Failed to fetch knowledge base' });
  }
});
```

### STEP 2: Fix the 500 on /api/solutions/generate

This is the OpenAI integration issue. The call is failing.

Find the `/api/solutions/generate` route and add DETAILED logging:

```javascript
app.post('/api/solutions/generate', async (req, res) => {
  console.log('=== SOLUTIONS GENERATE START ===');
  console.log('Request body:', JSON.stringify(req.body));
  
  try {
    const { problem, targetAudience, platform, videoPurpose } = req.body;
    
    // Log environment check
    console.log('Environment check:');
    console.log('- OPENAI_API_KEY exists:', !!process.env.OPENAI_API_KEY);
    console.log('- OPENAI_API_KEY prefix:', process.env.OPENAI_API_KEY?.substring(0, 10));
    console.log('- NODE_ENV:', process.env.NODE_ENV);
    console.log('- REPLIT_DEPLOYMENT:', process.env.REPLIT_DEPLOYMENT);
    
    // Check if API key exists
    if (!process.env.OPENAI_API_KEY) {
      console.error('CRITICAL: OPENAI_API_KEY is not set!');
      return res.status(503).json({ 
        error: 'AI service not configured. Please contact support.',
        debug: 'OPENAI_API_KEY missing'
      });
    }
    
    // Determine the correct base URL
    const isProduction = process.env.REPLIT_DEPLOYMENT || !process.env.REPLIT_DEV_DOMAIN;
    const baseUrl = isProduction 
      ? 'https://api.openai.com/v1'  // Use OpenAI directly in production
      : (process.env.AI_INTEGRATIONS_OPENAI_BASE_URL || 'https://api.openai.com/v1');
    
    console.log('Using OpenAI base URL:', baseUrl);
    console.log('Is production:', isProduction);
    
    // Make the OpenAI call
    console.log('Making OpenAI API call...');
    
    const response = await fetch(`${baseUrl}/chat/completions`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`
      },
      body: JSON.stringify({
        model: 'gpt-4',
        messages: [
          {
            role: 'system',
            content: 'You are a viral video script expert. Generate creative solutions and ideas.'
          },
          {
            role: 'user',
            content: `Generate video solutions for this problem: ${problem}
            Target Audience: ${targetAudience || 'General'}
            Platform: ${platform || 'TikTok'}
            Purpose: ${videoPurpose || 'Education'}`
          }
        ],
        max_tokens: 1000,
        temperature: 0.7
      })
    });
    
    console.log('OpenAI response status:', response.status);
    
    if (!response.ok) {
      const errorText = await response.text();
      console.error('OpenAI API error:', response.status, errorText);
      return res.status(500).json({ 
        error: 'AI service error. Please try again.',
        debug: `OpenAI returned ${response.status}`
      });
    }
    
    const data = await response.json();
    console.log('OpenAI response received successfully');
    
    const solutions = data.choices[0].message.content;
    
    console.log('=== SOLUTIONS GENERATE SUCCESS ===');
    res.json({ solutions });
    
  } catch (error) {
    console.error('=== SOLUTIONS GENERATE ERROR ===');
    console.error('Error name:', error.name);
    console.error('Error message:', error.message);
    console.error('Error stack:', error.stack);
    
    res.status(500).json({ 
      error: 'Failed to generate solutions. Please try again.',
      debug: error.message
    });
  }
});
```

### STEP 3: Add a health check endpoint

```javascript
app.get('/api/health', (req, res) => {
  res.json({
    status: 'ok',
    timestamp: new Date().toISOString(),
    environment: {
      NODE_ENV: process.env.NODE_ENV || 'not set',
      REPLIT_DEPLOYMENT: process.env.REPLIT_DEPLOYMENT || 'not set',
      OPENAI_API_KEY_EXISTS: !!process.env.OPENAI_API_KEY,
      OPENAI_API_KEY_LENGTH: process.env.OPENAI_API_KEY?.length || 0,
      OPENAI_API_KEY_PREFIX: process.env.OPENAI_API_KEY?.substring(0, 7) || 'NOT SET'
    }
  });
});
```

### STEP 4: Add a test endpoint for OpenAI

```javascript
app.get('/api/test-openai', async (req, res) => {
  console.log('Testing OpenAI connection...');
  
  if (!process.env.OPENAI_API_KEY) {
    return res.json({ 
      success: false, 
      error: 'OPENAI_API_KEY not set' 
    });
  }
  
  try {
    const response = await fetch('https://api.openai.com/v1/chat/completions', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.OPENAI_API_KEY}`
      },
      body: JSON.stringify({
        model: 'gpt-3.5-turbo',
        messages: [{ role: 'user', content: 'Say "API working" and nothing else.' }],
        max_tokens: 10
      })
    });
    
    const status = response.status;
    const body = await response.text();
    
    res.json({
      success: response.ok,
      status: status,
      response: body.substring(0, 200)
    });
  } catch (error) {
    res.json({
      success: false,
      error: error.message
    });
  }
});
```

### STEP 5: Make sure OPENAI_API_KEY is in deployment secrets

1. Go to Deployments panel
2. Click on the active deployment
3. Go to Settings/Configuration
4. Find Secrets section
5. Add: `OPENAI_API_KEY` = `sk-proj-...` (your actual OpenAI key from https://platform.openai.com/api-keys)
6. Redeploy

### STEP 6: Use OpenAI DIRECTLY, not Replit integration

The Replit integration proxy is unreliable. Change ALL OpenAI client initialization to use OpenAI directly:

```javascript
import OpenAI from 'openai';

const openai = new OpenAI({
  apiKey: process.env.OPENAI_API_KEY,
  baseURL: 'https://api.openai.com/v1'  // Direct to OpenAI, bypass Replit
});
```

Find and replace ANY instance of:
- `localhost:1106`
- `integrations.replit.com`
- `AI_INTEGRATIONS_OPENAI_BASE_URL`

With the direct OpenAI URL: `https://api.openai.com/v1`

### AFTER MAKING THESE CHANGES:

1. Republish the deployment
2. Wait for deployment to complete
3. Visit: https://viralscript.co/api/health
4. Visit: https://viralscript.co/api/test-openai
5. Share the results

## SUMMARY:

1. ✅ Fix /api/knowledge-base 403 error
2. ✅ Fix /api/solutions/generate 500 error with detailed logging
3. ✅ Add /api/health endpoint
4. ✅ Add /api/test-openai endpoint
5. ✅ Use OpenAI directly (https://api.openai.com/v1) instead of Replit integration
6. ✅ Make sure OPENAI_API_KEY is in deployment secrets
7. ✅ Republish

Please implement all of these changes now.